# pangea-ring0-os/Cargo.toml
[workspace]
members = [
    "kernel"
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

# (追加) Limineのブート可能イメージを生成するためのツール
[workspace.dependencies]
limine = "0.6.5"
